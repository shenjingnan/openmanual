{
  "name": "openmanual-docs",
  "private": true,
  "scripts": {
    "dev": "pnpm install && rm -rf .openmanual && openmanual dev",
    "build": "openmanual build"
  },
  "dependencies": {
    "openmanual": "file:.."
  }
}
