{
  "name": "openmanual-docs",
  "private": true,
  "scripts": {
    "dev": "pnpm install && rm -rf .openmanual && rm -rf docs-dist && openmanual dev",
    "build": "openmanual build"
  },
  "dependencies": {
    "openmanual": "file:.."
  }
}
