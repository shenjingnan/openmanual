{
  "name": "openmanual-docs",
  "private": true,
  "scripts": {
    "dev": "pnpm install && rm -rf .cache && openmanual dev",
    "build": "pnpm install && rm -rf .cache && rm -rf dist && openmanual build",
    "start": "npx -y serve dist -p 3000"
  },
  "dependencies": {
    "openmanual": "file:.."
  }
}
